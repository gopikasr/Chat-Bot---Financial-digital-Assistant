Read me file
Gopika S R
18412
BS Economic Science

Packages used
Make sure you have the below mentioned packages. If you are using colab you can use the following codes (I have already did in the Jupiter notebook)

!pip install graphviz
!pip install torchviz
import numpy as np
import random
import json
import torch
import torch.nn as nn
#importing nltk and necessary downloads
import nltk
nltk.download('punkt')
from nltk.stem.porter import PorterStemmer
from torch.utils.data import Dataset, DataLoader
nltk.download('all')

otherwise if you are using conda, install all these separately using conda command and make sure you are running it in that respective folder

ALSO, the data is "DataChatBot.json" file. If you didnt download it also, its fine since I have included
 the code with which I created the json file in the jupiter notebook.

Running the given jupiter notebook in the colab will give you full results. I managed to include everythging in the notebook and the remaining theory in the report.

